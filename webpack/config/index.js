import { version } from '../../src/version';

const config = {
  filename: 'search',
  licenseBanner: `Unified InstantSearch E-Commerce ${version} | © Algolia, Inc. and contributors; Apache-2.0 License | https://github.com/algolia/unified-instantsearch-ecommerce.\n`,
};

export default config;
